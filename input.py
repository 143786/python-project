# User input (function): is a feature that allows your script to interact with the user and with interaction the user
# execute the script so like when we call the script, the script will ask the user to enter some input. let's say
# they will enter a number and the script will get this number and will process it. will do some operations depending
# on what we have written inside the script, and then it will return some results back to users.
# and the user input feature is implemented through the input function.
# ** input is a build in function in python basically for interact with script.

    #input("What planet are you from")
# We can do somthing we this input, but at first we should store the value in a variable, then we can print it.

planet = input("What planet are you from")
print(planet)

